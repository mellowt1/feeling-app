# Scene lint

Generated by `design/v4/tools/lint.mjs` against `index.html`. See `design/v4/PLAN-scene-grammar.md` task 0.2.

| Place | `<use>` creatures (G12) | keyframes used | ambient-tier violations (>4px, <20s) (G9) | non-ambient durations used (G11) | distinct `--scene-*` vars | hard-coded colour hits (G7) |
|---|---|---|---|---|---|---|
| dock | 24 | 42 | 4 | 18 (2.4s, 3s, 4s, 6s, 8s, 9s, 11s, 14s, 23s, 26s, 90s, 96s, 120s, 130s, 150s, 180s, 250s, 3600s) | 3 | 1 |
| campo | 14 | 24 | 0 | 16 (1.7s, 4s, 4.4s, 4.6s, 11s, 28s, 40s, 60s, 80s, 90s, 120s, 150s, 180s, 300s, 420s, 3600s) | 4 | 0 |
| keywest | 15 | 44 | 2 | 22 (2.8s, 3.4s, 4s, 4.4s, 4.6s, 5s, 6s, 7s, 9s, 23s, 30s, 31s, 38s, 40s, 46s, 60s, 100s, 240s, 300s, 360s, 1800s, 3600s) | 2 | 1 |
| hawaii | 7 | 27 | 0 | 17 (4.4s, 5s, 6s, 7s, 9s, 23s, 31s, 36s, 60s, 90s, 120s, 150s, 200s, 240s, 300s, 320s, 900s) | 2 | 0 |
| tokyo | 0 | 13 | 4 | 8 (0.9s, 1.1s, 1.6s, 3.2s, 5s, 5.5s, 9s, 38s) | 0 | 0 |

## Ambient-tier violations (G9: translate > 4px on a loop under 20s)

- **dock**: `snapHover` (6px, 14s), `glitterDrift` (8px, 18s), `bobRain` (8px, 4s), `bobGust` (8px, 1.4s)
- **keywest**: `kwRuffle` (36px, 4.4s), `bobGust` (8px, 3.2s)
- **tokyo**: `tkDrift` (12px, 14s), `tkShimmer` (5px, 6s), `tkShimmer` (5px, 6s), `tkRings` (19.1px, 0.28s)

## Unscoped selectors (not prefixed by `.waves`, `html[data-place`, or `body.dock .waves`)

none found.

## Hard-coded colours inside `html[data-place="X"]` scene rules (G7)

- **dock**: 1 declaration(s) with a literal colour outside a `--` custom property.
- **keywest**: 1 declaration(s) with a literal colour outside a `--` custom property.
